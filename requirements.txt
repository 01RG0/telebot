# Core dependencies (required for Railway)
pyTelegramBotAPI==4.14.0
pymongo==4.6.1
python-dotenv==1.0.0

# GUI dependencies (only needed for local admin interface)
# Railway will skip these automatically
customtkinter==5.2.1
pandas==2.1.4
openpyxl==3.1.2

# Web Dashboard dependencies
Flask==3.0.0
gunicorn==21.2.0
werkzeug==3.0.1